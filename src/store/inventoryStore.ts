import { createHighlightStore } from '@/store/highlightStore'

/** Highlight signal for the backpack inventory button (see createHighlightStore). */
export const useInventoryStore = createHighlightStore()
