import { createHighlightStore } from '@/store/highlightStore'

/** Highlight signal for the renaissances log button (see createHighlightStore). */
export const useRebirthStore = createHighlightStore()
